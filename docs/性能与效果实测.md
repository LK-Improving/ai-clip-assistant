# KK剪映 · 性能与效果实测（M6）

> 由 `pnpm bench:metrics` 于 2026-09-20T01:34:01.811Z 在本机自动采集，测量代码 = scripts/bench/metrics.*，可复跑验证。
> 环境：10.0.26200 / Node v20.0.0 / ffmpeg=D:\Study\重点项目\AI剪映\apps\desktop\extraResources\ffmpeg\ffmpeg.exe

## TTS 缓存

| 指标 | 实测值 | 说明 |
|---|---|---|
| 未命中（网络合成+写盘+ffprobe 探测） | **1494ms** | 含模拟 RTT 200ms |
| 命中（内存 LRU） | **1ms** | 整体提速 1494.0x；扣除网络 RTT 后本地开销对比 1294.0x |
## 素材扫描

| 指标 | 实测值 | 说明 |
|---|---|---|
| 全量扫描 20 文件（逐文件 ffprobe+缩略图） | **21352ms** | added=20 |
| 增量重扫 20 文件（仅 stat size/mtime 比对） | **20ms** | 提速 1067.6x，updated=0 |
## 结构化输出防线

| 指标 | 实测值 | 说明 |
|---|---|---|
| 200 样本经「Zod 校验 + 重试 + 兜底」后产出合法简报 | **200/200** | 兜底 60 条，未捕获异常 0 条（对坏输出拦截率 100%） |
| 对照组：裸 JSON.parse 崩溃数 | **80/200** | 坏样本占比即无防线时的事故率 |
## LangGraph Checkpoint

| 指标 | 实测值 | 说明 |
|---|---|---|
| 前半程至中断（scan→brief→storyboard→interrupt） | **148ms** | 离线 Provider 全真实图执行 |
| 崩溃恢复：resumeFromCheckpoint 续跑后半程至完成 | **275ms** | 工程产出 ✓ |
## 大工程往返

| 指标 | 实测值 | 说明 |
|---|---|---|
| 110 片段工程保存（Zod 校验+落盘） | **21ms** | 回读 clipCount=110 |
| 读取（migrate+Zod 往返） | **40ms** | 内容合法 ✓ |

## 口径结论（替换 PDF 宣传数字）

- 「TTS 重复请求响应提升 10 倍以上」→ 以本表实测的缓存命中/未命中比为准（本机离线 mock 环境数字）；
- 「格式错误率降低 90% 以上」→ 以「结构化输出防线」组实测为准：200 混合坏样本经 Zod+重试+兜底后未捕获异常 0，对照裸 JSON.parse 崩溃数即为防线拦截量；
- 「制作周期从小时级到分钟级」「效率 5 倍」→ 定性表述 + 本表工程操作耗时（扫描/保存/checkpoint 恢复）佐证，不引用未经测量的倍数。
